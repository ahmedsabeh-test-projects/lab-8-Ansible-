plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

filters:
  tag:Name: ivolve

keyed_groups:
  - prefix: tag
    key: tags.Name

hostnames:
  - private-ip-address

compose:
  ansible_host: public_ip_address
